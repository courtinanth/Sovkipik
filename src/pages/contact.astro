---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Contact - Sovkipik">
  <div class="bg-forest text-white py-16">
    <div class="container mx-auto px-4 text-center">
      <h1 class="text-4xl font-bold mb-4">Contactez Sovkipik</h1>
      <p class="text-xl opacity-90">
        Une question ? Un témoignage sur un hérisson sauvé ?<br />
        Écrivez-nous à <a
          href="mailto:contact@sovkipik.com"
          class="underline hover:text-earth-light font-bold"
          >contact@sovkipik.com</a
        >
      </p>
    </div>
  </div>

  <section class="container mx-auto px-4 py-12 max-w-2xl">
    <div class="bg-white p-8 rounded-2xl shadow-lg border border-beige-dark/10">
      <form class="space-y-6">
        <div>
          <label for="name" class="block text-sm font-bold text-forest mb-2"
            >Votre Nom</label
          >
          <input
            type="text"
            id="name"
            name="name"
            class="w-full px-4 py-3 rounded-lg bg-beige-softer border border-earth/20 focus:border-forest focus:outline-none transition-colors"
            placeholder="Jean Dupont"
            required
          />
        </div>

        <div>
          <label for="email" class="block text-sm font-bold text-forest mb-2"
            >Votre Email</label
          >
          <input
            type="email"
            id="email"
            name="email"
            class="w-full px-4 py-3 rounded-lg bg-beige-softer border border-earth/20 focus:border-forest focus:outline-none transition-colors"
            placeholder="jean@exemple.com"
            required
          />
        </div>

        <div>
          <label for="subject" class="block text-sm font-bold text-forest mb-2"
            >Sujet</label
          >
          <select
            id="subject"
            name="subject"
            class="w-full px-4 py-3 rounded-lg bg-beige-softer border border-earth/20 focus:border-forest focus:outline-none transition-colors"
          >
            <option>J'ai une question sur un hérisson</option>
            <option>Problème technique sur le site</option>
            <option>Partenariat / Autre</option>
          </select>
        </div>

        <div>
          <label for="message" class="block text-sm font-bold text-forest mb-2"
            >Votre Message</label
          >
          <textarea
            id="message"
            name="message"
            rows="5"
            class="w-full px-4 py-3 rounded-lg bg-beige-softer border border-earth/20 focus:border-forest focus:outline-none transition-colors"
            placeholder="Bonjour..."
            required></textarea>
        </div>

        <button
          type="submit"
          class="w-full bg-highlight hover:bg-highlight-hover text-white font-bold py-4 rounded-xl shadow-md transition-transform transform hover:-translate-y-1"
        >
          Envoyer mon message
        </button>
      </form>
    </div>
  </section>
</Layout>
